你的角色：
你是一位资深的 Python 开发专家，擅长构建多智能体系统（multi-agent systems），并精通使用 Pydantic 进行数据结构化与校验。

项目背景：
我正在开发一个与大型 OWL 本体进行交互的多智能体系统（MAS）。该系统通过一个迭代循环（最多四次）来工作。在每次循环中，智能体会调用工具函数来查询本体知识，然后一个“验证器（Validator）”智能体会根据“完备性”、“一致性”、“准确性”等维度来评估查询结果，并生成一份反馈报告，用以指导下一次的迭代。

当前状况与代码：
我目前的反馈机制依赖于以下的 Pydantic 模型。系统会生成一个 ValidationReport 对象，其中包含一些高级别的文本信息和改进建议。

核心痛点：
当前的反馈模型效率不高。improvement_suggestions 字段里只是纯文本字符串。这导致下一轮迭代的智能体无法知道是哪一个具体的工具调用（Tool Call）出了问题，也无法得知应该如何精确地修改该调用的参数。例如，一个像“尝试使用该实体的同义词”这样的建议过于模糊，智能体难以执行。这最终导致了低效、有时甚至是重复的试错式迭代。

目标：让反馈变得“可操作”且“可追溯”
你的任务是重构上述 Pydantic 模型，使反馈变得高度结构化、可直接追溯到具体的工具调用，并且能够被下一轮迭代中的智能体立即解析并执行。我们需要在“问题”和“机器可读的解决方案”之间建立一个明确的链接。

原始情况描述：目前我在开发一个multiagents系统。用工具函数查询owl本体知识增强响应。一个问题比较困扰我。本体较大（10k+classes和1-k+properties），MAS会迭代至多四次，每次迭代只会携带反馈信息。这导致一个问题。每次迭代对前一次调用的工具和操作的类是未知的，这种清空有时很好，能够促进系统探索（如未指定的缩写）。但是有时会阻碍模型知道之前给出的查询类列表是否合理（比如一个类查询为空，下次应该换他的近义词而不是重复。因为本体是LLM构建的，质量控制较低，有一些近义词。或这比如一个查询查到了数据，下次应该保持不变）。实际上按照设计，我是希望将这个事情在validation中搞定的。但是有一个悖论，不查询情况下validator不知道查询的结果是否完备或正确。因此其作用远比想象的有限（但也确实有作用）。而且即便validator给出正确的validation，也没有包含关于具体哪个工具，传入哪个参数是正确/不正确的信息。导致MAS系统的迭代效果没有达到预期。